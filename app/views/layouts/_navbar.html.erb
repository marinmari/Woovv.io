<nav class="navbar navbar-expand-lg" style="display:flex; justify-content:space-between; ">
  <div style="display:flex; justify-content:space-between">
    <%= link_to "Woovv", root_path, class:"navbar-brand"%>
    <ul class="navbar-nav">
      <li class="nav-link">
      </li>
      <% if !current_user%>
        <li class="nav-link">
          <a href="https://rz038zhvlso.typeform.com/to/fOfiofVp" class="nav-link">Proposer mes coworkings</a>
        </li>
      <% else%>
      <% end %>
    </ul>
  </div>
  <div class="dropdown my-account-dropdown">
    <% if current_user && current_user.avatar.attached?%>
      <div class = "navbar-img">
        <%= image_tag current_user.avatar, alt: 'avatar' %>
      </div>
    <% end %>
    <button class="dropdown-btn" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
      <i class="fas fa-bars"></i>
    </button>
    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuButton">
      <%# <a class="dropdown-item" href="#">Action</a> %>
      <%= link_to "Trouver un coworking", coworkings_path, class:"btn btn-primary" %>
      <div class="dropdown-divider"></div>
      <% if !current_user%>
        <%= link_to "Connexion", new_user_session_path, class:"dropdown-item" %>
        <%= link_to "Inscription", new_user_registration_path, class:"dropdown-item" %>
      <% else %>
        <% if current_user.bookings.count > 0%>
          <%= link_to "Mes réservations", user_bookings_path(current_user.id), class:"dropdown-item"  %>
        <% end %>
        <%= link_to "Mon compte",  user_path(current_user.id),class:"dropdown-item" %>
        <%= link_to "Deconnexion",destroy_user_session_path, method: :delete, class:"dropdown-item" %>
        <div class="dropdown-divider"></div>
        <div class = "coworking-manager">
          <% if current_user.managed_coworkings.count > 0%>
            <p class="dropdown-item">Coworking manager</p>
            <%= link_to "Mes coworkings", coworking_manager_coworkings_path, class:"dropdown-item"  %>
            <% if Booking.where(coworking_id:current_user.managed_coworkings.select(:id)).count > 0%>
              <%= link_to "Gérer les réservations", coworking_manager_bookings_path, class:"dropdown-item"  %>
            <% end %>
          <% end %>
        </div>
        <%# changer le path de mon compte %>
      <% end %>
    </div>
  </div>
</nav>
