<%= form_with(model: private_set_up, local: true) do |form| %>
  <% if private_set_up.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(private_set_up.errors.count, "error") %> prohibited this private_set_up from being saved:</h2>
      <ul>
        <% private_set_up.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
